describe('Rentals Page', () => {
    it('Should show the modal when button is clicked', () => {
        // const button =
        fireEvent(click, button);
        //expect(screen.getByText('modal title')).toBeInTheDocumet();
    })
})

//res:{
    data: [],
    status: 404, 200, 
}

